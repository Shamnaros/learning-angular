import { BootstrapList } from './bootstrap-list';

describe('BootstrapList', () => {
  it('should create an instance', () => {
    expect(new BootstrapList(1,'j','keyframes','l')).toBeTruthy();
  });
});
